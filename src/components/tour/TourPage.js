import React,{Component} from 'react';
import {Link} from 'react-router';

class TourPage extends Component {
  render() {
    return (
      <div>
   <br /><br /><br /><br />
    <div>Nothing here yet.</div>
        </div>
    );
  }
}

export default TourPage; //when someone imports the file...it gets a reference to the HomePage className, because I exported it
